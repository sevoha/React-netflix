export default interface Series {
        rank: number;
        img: string;
        name: string;
        startYear: number;
        rate: number;
        description: string;
        running: boolean;
}